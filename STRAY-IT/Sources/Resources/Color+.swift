import SwiftUI

public extension Color {
    static let accent: Self = .init(.accent)
    static let background: Self = .init(.background)
    static let distanceFont: Self = .init(.distanceFont)
    static let route: Self = .init(.route)
}
