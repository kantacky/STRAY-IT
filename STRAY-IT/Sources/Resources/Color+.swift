import SwiftUI

public extension Color {
    static let primaryBackground: Self = .init(.primaryBackground)
    static let primaryFont: Self = .init(.primaryFont)
    static let secondaryBackground: Self = .init(.secondaryBackground)
    static let secondaryFont: Self = .init(.secondaryFont)
    static let accentBackground: Self = .init(.accentBackground)
    static let accentFont: Self = .init(.accentFont)
}
